use super::{configuration, Error};
use crate::{apis::ResponseContent, models};
use async_trait::async_trait;
use reqwest;
use serde::{Deserialize, Serialize};
use std::sync::Arc;

#[async_trait]
pub trait OffExchangesApi: Send + Sync {
  async fn unsupported(&self) {}
}

pub struct OffExchangesApiClient {
  #[allow(dead_code)]
  configuration: Arc<configuration::Configuration>,
}

impl OffExchangesApiClient {
  pub fn new(configuration: Arc<configuration::Configuration>) -> Self {
    Self { configuration }
  }
}

impl OffExchangesApi for OffExchangesApiClient {}
